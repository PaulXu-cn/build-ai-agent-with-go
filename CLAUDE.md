# CLAUDE.md

本项目是一套「用 Go 从零手写一个 AI Agent（类 Claude Code）」的教学课程。

核心目标**不是复刻产品，而是讲透一个 AI Agent 到底怎么工作**。

## 一句话主线

> **Agent = 一个 while 循环。**
> 把消息发给 LLM → 模型返回"我要调工具 X(参数)" → 程序执行 X → 结果回传 → 再问模型 → … → 直到模型说"做完了"。

## 硬性约束（违反即返工）

1. **零三方依赖**：只允许 Go 标准库。禁止 `anthropic-sdk-go`、`enio`、`viper`、`cobra`、`urfave/cli` 等一切外部库。
   原因：避免技术黑盒。调用 LLM 一律用 `net/http` 手写 HTTP + `encoding/json` 手写协议。
2. **Go 1.18 起步**：`go.mod` 写 `go 1.18`。**不使用 1.18 之后才加入的语言特性或标准库**，
   例如 `slices`/`maps`(1.21)、`errors.Join`(1.20)、内建 `min`/`max`(1.21)、`atomic.Pointer`(1.19)。
   泛型仅限 1.18 语法。写之前先想：这个特性/包在 1.18 里有没有？
3. **每章一个独立可执行文件夹**：不做单一入口。各章自包含，`go run ./<章节>` 直接跑。
   允许章节之间少量重复代码 —— 教学上"每一章都能独立读懂"优先于 DRY。
4. **注释完备、中文、讲"为什么"**：注释解释原理与设计意图（为什么这样设计、这个字段什么意思），
   不逐行翻译语法。目标是一个没学过 agent 的人能顺着注释读懂。
5. **手写优先**：核心代码手写；只在机械处（如 JSON 结构体字段对齐、switch 分支补全）用自动补全。
6. **不硬编码密钥**：API key、模型名一律走环境变量，禁止写死在代码里。

## 环境变量约定

| 变量                 | 作用                              | 默认值                        |
| -------------------- | --------------------------------- | ----------------------------- |
| `ANTHROPIC_API_KEY`  | 鉴权（mock 模式可不设）           | 无                            |
| `ANTHROPIC_BASE_URL` | API 地址，指向 mock 即可离线跑    | `https://api.anthropic.com`   |
| `ANTHROPIC_MODEL`    | 模型 ID                           | `claude-opus-4-8`             |

- 请求头固定三项：`x-api-key`、`anthropic-version: 2023-06-01`、`content-type: application/json`。
- 模型 ID 用不带日期后缀的别名（如 `claude-opus-4-8`，不要写 `...-2025xxxx`）。
- 练习想省钱/更快可 `ANTHROPIC_MODEL=claude-haiku-4-5`（最快最便宜）或 `claude-sonnet-4-6`。
- 不要给请求加 `temperature`/`top_p`/`top_k`（新版模型已移除，会 400）。

## 课程内容

课程的具体章节计划、目录结构、学习路线，见 **README.md**。本文件只定规则，不列章节。

## 每章必备（三件套，缺一不可）

1. **`main.go`**：可独立运行（`go run ./<章节>`），代码注释完备。
2. **`README.md`**：讲清楚「本章讲什么概念 → 和上一章什么关系 → 怎么跑（真实/mock 两种）→ 预期输出」。
3. **Makefile target**：根 `Makefile` 里给本章加一个 `run-<章节>` 目标。

## 教学写作规范

- 每个新概念先给「一句话人话」，再上代码；代码里的注释讲"为什么"。
- 输出 JSON 时用 `json.MarshalIndent` 或格式化打印，让学员能看清请求/响应结构（黑盒被揭开）。
- 错误处理要显式、读得懂：打印 `statusCode` + 响应体，不要吞错。
- 安全相关（`06`/`07` 章的文件路径、命令执行）必须写清边界：工作目录限制、超时、路径校验。

## 交付节奏

- 每个阶段结束都要 `make` 全量能编译、能跑通，再进入下一阶段。
- 章节顺序即依赖顺序：后面的章建立在前面章的概念之上，但**代码不互相 import**。
